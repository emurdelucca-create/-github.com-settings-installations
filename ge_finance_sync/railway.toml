[build]
builder = "NIXPACKS"

[deploy]
# Runs once at container start; Railway Cron Service calls this on schedule.
startCommand = "python slack_sync.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
